version: '3'

services:
  zookeeper:
    image: confluentinc/cp-zookeeper:latest
    hostname: zookeeper
    container_name: zookeeper
    ports:
      - "2181:2181"
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000

  kafka:
    image: confluentinc/cp-kafka:latest
    hostname: kafka
    container_name: kafka
    
    ports:
      - "9092:9092"
    depends_on:
      - zookeeper
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9093,PLAINTEXT_HOST://kafka:9092
      # KAFKA_METRIC_REPORTERS: io.confluent.metrics.reporter.ConfluentMetricsReporter
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_CONFLUENT_LICENSE_TOPIC_REPLICATION_FACTOR: 1
      CONFLUENT_METRICS_REPORTER_BOOTSTRAP_SERVERS: kafka:9093
      CONFLUENT_METRICS_REPORTER_ZOOKEEPER_CONNECT: zookeeper:2181
      CONFLUENT_METRICS_REPORTER_TOPIC_REPLICAS: 1
      CONFLUENT_METRICS_ENABLE: "true"
      CONFLUENT_SUPPORT_CUSTOMER_ID: "anonymous"
    # volumes:
    #   - /var/lib/kafka/data:/var/lib/kafka/data

  scmuifrontend:
    image: frontend:v1
    ports:
      - "80:80"
    environment:
      - REACT_APP_GOOGLE_API_KEY=6LdLllgpAAAAAMexecY2HM1tlocPTGTpXTvWu8fc
      - REACT_APP_API_URL=http://localhost:8080
      - REACT_APP_GEMINI_API_KEY=AIzaSyAiVlCW7sO6fRI48f8ClCICjfQE9M4WXSc

  socket:
    image: socket:v1
    ports:
      - "12345:12345"  # Ensure this port does not conflict with others
  producerandconsumer:
    image: producerandcosumer:v1
    environment:
      - SPRING_CLOUD_STREAM_KAFKA_BINDER_BROKERS=kafka:9092
      - SOCKET_HOST=socket
      - SOCKET_PORT=12345
      - SPRING_KAFKA_CONSUMER_GROUP_ID=my-consumer-group
      - SPRING_DATA_MONGODB_DATABASE=JobSearch
      - SPRING_DATA_MONGODB_URI=mongodb+srv://codewithadurintiashok:Ashok12345@scmserver.rcyhq.mongodb.net/JobSearch
      - SPRING_KAFKA_PRODUCER_BOOTSTRAP_SERVERS=kafka:9092

    depends_on:
      - kafka

  scmbackend:
    image: scmapibackend:v1
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATA_MONGODB_URI=mongodb+srv://codewithadurintiashok:Ashok12345@scmserver.rcyhq.mongodb.net/
      - SPRING_DATA_MONGODB_DATABASE=JobSearch
      - SPRING_MAIL_USERNAME=scmxperttest@gmail.com
      - SPRING_MAIL_PASSWORD=lbuiwlccylelsjme
      - SPRING_MAIL_PORT=587
      - SPRING_MAIL_HOST=smtp.gmail.com
      - SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH=true
      - SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS=true
      - SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE=true
      - SPRINGDOC_ENABLE_SPRING_SECURITY=false
      - APPLICATION_RESET_PASSWORD_URL=http://localhost/reset-password?token=
      - APPLICATION_SWAGGER_URL=/swagger-ui/index.html
      - APPLICATION_PASETO_FOOTER=scmServer footer contains meta data
      - APPLICATION_PASETO_SECRETKEY=WfvKvfSqJRKkGRe54NvNyH9M4HAyHNwd

  control-center:
    image: confluentinc/cp-enterprise-control-center:latest
    hostname: control-center
    container_name: control-center
    ports:
      - "9021:9021"
    depends_on:
      - kafka
    environment:
      CONTROL_CENTER_BOOTSTRAP_SERVERS: "kafka:9093"
      CONTROL_CENTER_ZOOKEEPER_CONNECT: "zookeeper:2181"
      CONTROL_CENTER_REPLICATION_FACTOR: 1
      CONTROL_CENTER_INTERNAL_TOPICS_PARTITIONS: 1
      CONTROL_CENTER_MONITORING_INTERCEPTOR_TOPIC_PARTITIONS: 1
      CONFLUENT_METRICS_TOPIC_REPLICATION: 1
      PORT: 9021
